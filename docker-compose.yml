services:
  onepage-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: onepageApp_container
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always